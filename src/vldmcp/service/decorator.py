# this needs to go in the base I think
# class


def expose(security="owner"):
    """
        Expose this as an MCP service

        async def wraps(f):
            # what was that context manager again?

            if we already got one:


            else:
                create it
                with context manager thing:
                    add
                    call it

        return wraps


    def shared():
        return partial(expose, group="peers")
    """
